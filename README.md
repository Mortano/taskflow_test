# Example project illustrating a problem I have with using cpp-taskflow

## How to build

`cd build/debug && cmake -DCMAKE_BUILD_TYPE=debug ../../ && make && make install`

## How to run

`cd bin/debug && ./executable`
